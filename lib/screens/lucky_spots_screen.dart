import 'package:flutter/material.dart';

class LuckySpotsScreen extends StatelessWidget {
  const LuckySpotsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('명당 화면'),
    );
  }
}
